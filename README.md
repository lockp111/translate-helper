# Translate Helper - VSCode 翻译助手

一个基于 **大语言模型 (LLM)** 的智能 VSCode 翻译助手插件，支持划词翻译和变量命名建议。

## 功能特性

### 🌐 智能翻译
- **多模型支持** - 集成全球主流大模型：
  - OpenAI (GPT-4/3.5)
  - Claude (Anthropic)
  - DeepSeek (深度求索)
  - Google Gemini
  - 智谱 AI (GLM-4)
  - Kimi (月之暗面)
  - 硅基流动 (SiliconFlow)
- **极速响应** - 优化的 API 调用，秒级返回翻译结果
- **自然语言翻译** - 理解上下文进行准确翻译，而非简单的机翻
- **多种语言支持** - 支持中文、英文、日文、韩文、法文、德文、西班牙文、俄文
- **快捷键支持** - `Alt+T` 快速翻译选中文本
- **右键菜单** - 选中文字后右键点击"翻译选中文本"

### 💡 变量命名建议
- **AI 智能生成** - 根据中文描述生成专业的英文命名
- **JSON Mode** - 强制模型返回结构化数据，确保建议格式稳定
- **智能风格判断** - 根据文件类型自动推荐合适的命名风格：
  - JavaScript/TypeScript: camelCase
  - Python: snake_case
  - CSS/HTML: kebab-case
  - Java/C#: camelCase / PascalCase
- **多种命名策略** - 提供多个高质量建议，包含动词+名词、简洁版、数据导向等
- **快捷键支持** - `Alt+N` 快速获取命名建议
- **一键替换** - 直接替换编辑器中的文本

### 🎯 交互体验
- **行内 CodeLens** - 选中文本后，在代码行上方自动显示操作按钮（可配置）
- **快捷键 & 右键菜单** - 符合各种操作习惯
- **结果面板** - 极简风格展示，支持一键复制和替换
- **灵活配置** - 自定义 API Key、目标语言、建议数量等

## 安装

在 VS Code 插件市场搜索 "Translate Helper" 安装即可。

或者从源码运行：
1. 克隆本仓库 `git clone ...`
2. `npm install`
3. 按 `F5` 调试运行

## 配置指南

在 VS Code 设置中搜索 `translateHelper`，首先选择 **Provider**：

### 1. 选择 AI 服务商 (Provider)
从下拉列表中选择你使用的服务商：`openai` / `claude` / `gemini` / `deepseek` / `kimi` / `zhipu` / `siliconflow`

### 2. 配置对应的 API Key
根据选择的 Provider，填写对应的 API Key：

- **OpenAI**: 填写 `translateHelper.openaiApiKey` (及可选的 `openaiBaseUrl` / `openaiModel`)
- **Claude**: 填写 `translateHelper.claudeApiKey`
- **DeepSeek**: 填写 `translateHelper.deepseekApiKey`
- **Gemini**: 填写 `translateHelper.geminiApiKey`
- **Kimi (月之暗面)**: 填写 `translateHelper.kimiApiKey` (API Key 从 [Moonshot AI](https://platform.moonshot.cn/) 获取)
- **智谱 AI (GLM)**: 填写 `translateHelper.zhipuApiKey` (API Key 从 [智谱 AI](https://open.bigmodel.cn/) 获取)
- **硅基流动**: 填写 `translateHelper.siliconflowApiKey` (API Key 从 [SiliconFlow](https://siliconflow.cn/) 获取)

### 其他配置
- **Target Language** - 目标语言（默认: zh - 中文）
- **Naming Count** - 变量命名建议数量（默认: 3）
- **Enable Quick Actions** - 是否显示行内 CodeLens 按钮（默认: true）

## 使用方法

### 翻译功能
1. **快捷键**: 选中文本 -> 按 `Alt+T`
2. **CodeLens**: 选中文本 -> 点击上方 `🌐 翻译` 按钮
3. **右键菜单**: 选中文本 -> 右键 -> `翻译选中文本`

### 变量命名功能
1. **快捷键**: 选中中文描述 -> 按 `Alt+N`
2. **CodeLens**: 选中文本 -> 点击上方 `💡 命名` 按钮
3. **右键菜单**: 选中文本 -> 右键 -> `变量命名建议`

## 支持的文件类型与命名风格

插件会自动根据当前文件类型推荐命名风格：

| 文件扩展名           | 推荐风格   | 示例          |
| -------------------- | ---------- | ------------- |
| .js, .ts, .jsx, .tsx | camelCase  | `getUserList` |
| .java, .cs, .go      | camelCase  | `userName`    |
| .py, .rb, .php, .sql | snake_case | `user_name`   |
| .css, .scss, .html   | kebab-case | `user-name`   |
| .vue                 | camelCase  | `userList`    |

## 许可证

MIT License
